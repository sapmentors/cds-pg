{
  "scripts": {
    "start": "cds run",
    "copycds:db": "copyfiles -a ./db/* ./db/**/* scp-deployment",
    "copycds:srv": "copyfiles -a ./srv/* ./srv/**/* scp-deployment"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "database"
      },
      "database": {
        "impl": "../../../",
        "model": [
          "srv"
        ]
      }
    }
  }
}
